# Copy this file to .streamlit/secrets.toml and adjust for your environment.
# secrets.toml is gitignored and will NOT be committed.

# --- Local development (Mac / Homebrew PostgreSQL) ---
# Homebrew Postgres typically uses your macOS username, no password:
# DATABASE_URL = "postgresql+psycopg2://YourMacUser@localhost:5432/runseason"

# --- Local development (Linux / Docker) ---
# DATABASE_URL = "postgresql+psycopg2://postgres:postgres@localhost:5432/runseason"

# --- Production / Streamlit Cloud ---
# DATABASE_URL = "postgresql+psycopg2://USER:PASSWORD@HOST:5432/runseason?sslmode=require"

# Pick one of the above and uncomment it:
DATABASE_URL = "postgresql+psycopg2://localhost:5432/runseason"

SECRET_KEY = "replace-with-long-random-string"
